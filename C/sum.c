#include<stdio.h>
#include<stdlib.h>
int main(void)
{
    int a,b,sum;
    printf("enter two number for finding the sum");
    scanf("%d%d", &a,&b);
    sum=a+b;
    printf("the sum of two number is %d",sum);
}